/*
package GUI;

import javafx.scene.control.Button;

public class LogoutButton extends Button {

    public LogoutButton(MainApp mainApp) {
        super("Logout");

        this.setOnAction(e -> {
            mainApp.logout();
        });

        this.setStyle("-fx-background-color: #c0392b; -fx-text-fill: white;");
    }
}*/
        //to add logout button in any page
/*
    VBox layout = new VBox();
    layout.setSpacing(10);
    layout.setPadding(new Insets(20));

    // Add logout button
    LogoutButton logoutButton = new LogoutButton(mainApp);
    layout.getChildren().add(logoutButton);

 */